This directory is used to create some call graphs using doxygen. So it's only for visualization of the program flow.

The gbdt code is non-working, it was quickly stripped down. All noise, (like logging related stuff, which would clutter the callgraphs) is removed. 